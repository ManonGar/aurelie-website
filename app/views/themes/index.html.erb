<div class="theme-cards">
  <% @themes.each do |theme| %>
    <div class="theme-card">
      <div id="card-image">
        <%= link_to (cl_image_tag theme.photo.key, height: 300, width: 450), theme_path(theme) %>
      </div>
      <div class="card-title">
         <%= theme.name.upcase %>
      </div>
    </div>
  <% end %>
</div>












